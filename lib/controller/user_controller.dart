// ignore_for_file: avoid_print

// import 'dart:io';
// import 'package:http/http.dart' as http;

// class UserCntroller {
// 
// 
/*
//http://localhost:8000/
//192.168.43.197
  Future<void> uploadAndIndexPDF() async {
    var url = Uri.parse(
        'http://192.168.43.197:8000/'); // Replace with your Laravel API URL

    var request = http.MultipartRequest('POST', url);

    // Set headers if needed, for example:
    // request.headers['Authorization'] = 'Bearer YOUR_ACCESS_TOKEN';

    request.files
        .add(await http.MultipartFile.fromPath('pdf', 'path_to_your_pdf_file'));
    request.fields['title'] = 'Your PDF Title';

    var response = await request.send();

    if (response.statusCode == 200) {
      print('PDF uploaded and indexed successfully');
      print('Document ID: ${await response.stream.bytesToString()}');
    } else {
      print('Failed to upload and index PDF');
    }
  }
*/
// 
// 
// }
